services:
  cote-bot:
    build: .
    container_name: cote-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # store.json 영속성 보장
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
